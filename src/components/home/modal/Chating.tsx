import React from 'react';
import { ChatingBlock, Block } from '../../styles/home/modal/Chating.style';

const Chating = () => {
  return (
    <ChatingBlock>
      <Block></Block>
    </ChatingBlock>
  );
};

export default React.memo(Chating);
